fun main(){

    // Basic lambda expression
    // 1-> With parameter and return type
   /*  val add:(Int,Int)->Int={a,b->a+b}
    val result=add(5,6)
    println(result) 

    // 2-> Parameter with no return value
    val add2:(Int,Int)->Unit={a,b->println(a+b)}
    add2(5,6) 

    // 3-> No parameter with return type
    val add3:()->String={"Mayank"}
    println(add3.invoke()) 

    // 4-> No parameter no return type
    val add4:()->Unit={println("I'm Mayank")}
    add4.invoke() 

    //Anonymous function
    val numbers=listOf(1,2,3,4,5)
    val square=fun(x:Int):Int{
        return x*x
    }
    val squarednumber=numbers.map(square)
    println(squarednumber) 

    // Types of anonmyous function

    // 1-> With parameter and return type
    val add=fun(x:Int,y:Int):Int{return x+y}
    println(add(3,4)) 

    // 2-> With parameter and without return type
    val add2=fun(a:Int,b:Int){println (a+b)}
    add2(5,4)

    // 3-> Without parameter and with return type
    val msg=fun():String{return "Mayank"}
    println(msg()) 

    // 4-> Without parameter and without return type
    val msg2=fun(){println("Mayank")}
    msg2() */
}